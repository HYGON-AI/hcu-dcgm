# Go 测试目录约定

- 可通过公开 Go API、HTTP 接口或命令行入口验证的黑盒与集成测试，统一放在 `samples/tests/<领域>/`。
- 必须访问包内未导出实现的单元测试保留在对应包目录，避免为测试而扩张生产 API；文件名仍使用 `*_test.go`。
- 新测试优先从 `samples/tests/` 开始设计；只有无法通过公开边界验证时，才在生产包内增加单元测试。

运行全部测试：

```bash
go test ./pkg/... ./samples/tests/...
```